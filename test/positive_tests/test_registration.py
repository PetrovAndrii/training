import string
import random
"""
def random_email(prefix, maxlen):
    symbols = string.ascii_letters + string.digits
    return prefix + "".join([random.choice(symbols) for i in range(random.randrange(maxlen))])


def test_new_account(app):
    last_name="test"
    first_name="uz"
    email = random_email("uz_test", 3) + "@yopmail.com"
    password = "P@ssw0rd"
    app.regist_group.new_account(first_name, last_name, email,  password)
    print("")
    print("email:", email)
"""